[project]
name = "autorag-local"
version = "0.1.0"
description = "AutoRAG local development setup and verification"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "autorag>=0.3",
    "httpx>=0.27",
    "pandas>=2.0",
]
